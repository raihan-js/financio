
// Mock Firebase services for now
// This will avoid initialization errors while we focus on the UI
const app = {};
const auth = {};
const firestore = {};
const storage = {};

export { auth, firestore, storage };
export default app;